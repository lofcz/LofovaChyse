<style>
    .ui-chatbox {
        position: fixed;
        bottom: 0;
        padding: 2px;
        background: #CCCCCC;
    }

    .ui-chatbox-titlebar {
        padding: 3px;
        height: 40px;
        cursor: pointer;
    }

    .ui-chatbox-content {
        padding: 0px;
        margin: 0px;
        border: 0px;
    }

    .ui-chatbox-log {
        padding: 3px;
        height: 250px;
        overflow-y: auto;
        overflow-x: hidden;
        background: #FFFFFF;
    }

    .ui-chatbox-input {
        padding: 3px;
        border-top: 1px solid grey;
        overflow: hidden;
    }

    .ui-chatbox-input-box {
        margin: 5px;
        border: 2px solid lightgrey; /* #6699FF */
        padding: 2px;
        height: 50px;
    }

    .ui-chatbox-icon {
        float: right;
    }

    .ui-chatbox-input-focus {
        border-color: #6699FF;
    }

    .ui-chatbox-msg {
        margin-top: 10px;
        float: left;
        clear: both;
        /* Source: http://snipplr.com/view/10979/css-cross-browser-word-wrap */
        white-space: pre-wrap; /* CSS3 */
        white-space: -moz-pre-wrap; /* Firefox */
        white-space: -pre-wrap; /* Opera <7 */
        white-space: -o-pre-wrap; /* Opera 7 */
        word-wrap: break-word; /* IE */
    }
</style>


<link rel="stylesheet" href="http://magma.cs.uiuc.edu/wenpu1/css/ui-lightness/jquery-ui-1.8.2.custom.css" />
<script type="text/javascript" src="http://magma.cs.uiuc.edu/wenpu1/js/jquery-ui-1.8.2.custom.min.js"></script>
<script type="text/javascript" src="http://magma.cs.uiuc.edu/wenpu1/js/jquery.ui.chatbox.js"></script>
<script type="text/javascript" src="http://magma.cs.uiuc.edu/wenpu1/js/chatboxManager.js"></script>

<link  rel="stylesheet" href="https://atomiks.github.io/tippyjs/css/prism.css" />


<style>
    html {
        font-size: 15px;
    }

    a:focus {
        text-decoration: none;
    }

    a span {
        color: #528cff;
    }

    a i {
        color: #00caf9;
    }

    table, p, ul {
        font-size: 1.1rem;
        line-height: 1.7;
    }

    h4 {
        margin: 2rem 0 1rem;
        font-weight: 600;
        line-height: 1.5;
    }

    code, pre {
        position: relative;
        background-color: #f1f5ff;
        color: #2d61df;
        margin-bottom: 1rem;
        line-height: 1.7;
        overflow-wrap: break-word;
    }

    pre {
        padding: 0.5rem 1rem;
    }

    h2 {
        font-size: 2.25rem;
        margin: 1rem 0 3rem;
        text-align: center;
    }

    strong {
        font-weight: 700;
    }

    h5 {
        line-height: 1.5;
        margin: 0;
    }

    .alert.alert-info {
        color: #0085ff;
        background-color: #f5fcff;
        border-color: #d6eaff;
        margin: 2rem 0;
    }

    .badge.badge-default {
        background-color: transparent;
        border: 1px solid #738fa1;
        color: #738fa1;
    }

    a:hover {
        text-decoration: none;
    }

    .material-icons {
        cursor: default;
        font-size: 1.1em;
        vertical-align: middle;
        position: relative;
        top: -0.05em;
    }

    .icon-left {
        margin-right: 0.25em;
    }

    .icon-right {
        margin-left: 0.25em;
    }

    .icon-big {
        font-size: 2em;
    }

    .opacity-low {
        opacity: 0.8;
    }

    .docs {
        margin-top: 1rem;
    }
    .btn-docs-version {
        margin: 0 0.25rem;
        background: transparent;
        cursor: pointer;
        color: white;
        transition: all 0.2s ease;
    }
    .btn-docs-version:hover {
        background: white;
    }
    .btn-docs-version-current {
        background: white;
        color: #007bff;
    }

    .hero-badge {
        border: 1px solid rgba(223, 255, 123, 0.5);
        padding: 0.5rem 1rem;
        border-radius: 4px;
        color: #fff;
        font-weight: 500;
        font-size: 1.2rem;
        margin: 0 0.5rem 1rem;
        min-width: 104px;
    }

    .hero-badge:nth-child(1), .hero-badge:nth-child(3) {
        border-color: transparent;
    }

    .hero-badge:nth-child(1) span {
        color: #fff9b0;
        background: -webkit-linear-gradient(135deg, #fff9b0, #7dffc3);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
    }

    .hero-badge:nth-child(1) i {
        color: pink;
    }

    .hero-badge:nth-child(2) {
        border-color: rgba(255,255,255,0.25);
    }

    .hero-badge:nth-child(2) i {
        color: #ffe79e;
    }

    .hero-link {
        color: inherit;
        text-decoration: none;
    }
    .hero-link span {
        color: #68f6ff;
        background: -webkit-linear-gradient(left,#bef9ff, #9aecff);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
    }
    .green-text {
        color: #3ba065;
        font-weight: 600;
    }

    .badge {
        font-size: 80%;
    }

    .tippy-popper[data-html] .tippy-tooltip {
        padding: 2rem;
        box-shadow: 0 4px 80px -8px rgba(0, 20, 60, 0.25);
        max-width: 400px;
        font-size: 1.2rem;
        border-radius: 10px;
    }

    .logo {
        width: 80px;
        margin-left: -40px;
        position: absolute;
        top: 60px;
        left: 50%;
        -webkit-user-select: none;
        -moz-user-select: none;
        -ms-user-select: none;
        user-select: none;
    }

    :focus {
        outline: auto 5px #5ba8ff !important;
        outline: auto 5px -webkit-focus-ring-color !important;
    }

    :focus:not(.focus-ring), input:focus {
        outline: 0 !important;
    }

    .btn {
        font-size: 1.1rem;
        font-family: inherit;
    }

    .result-box {
        padding: 1rem;
        background-color: #f5f6f9;
        margin-bottom: 1rem;
    }

    .result-box p {
        margin: -0.5rem 0 0.5rem;
        font-weight: 500;
    }

    .hero {
        padding: 8rem 0 4rem;
        color: white;
        text-align: center;
        -webkit-transition: opacity 1.5s cubic-bezier(0, 0, 0.15, 1.01), -webkit-transform 1.5s cubic-bezier(0, 0, 0.15, 1.01);
        transition: opacity 1.5s cubic-bezier(0, 0, 0.15, 1.01), -webkit-transform 1.5s cubic-bezier(0, 0, 0.15, 1.01);
        transition: opacity 1.5s cubic-bezier(0, 0, 0.15, 1.01), transform 1.5s cubic-bezier(0, 0, 0.15, 1.01);
        transition: opacity 1.5s cubic-bezier(0, 0, 0.15, 1.01), transform 1.5s cubic-bezier(0, 0, 0.15, 1.01), -webkit-transform 1.5s cubic-bezier(0, 0, 0.15, 1.01);
    }

    .hero-heading {
        display: inline-block;
        font-weight: 300;
        font-size: 4rem;
        padding: 0.5rem;
        color: #7dffc3;
        background: -webkit-linear-gradient(135deg, #fff9b0, #7dffc3);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
    }

    .tippy-tooltip[data-template-id="#template"] {
        padding: 2rem;
        transition-timing-function: cubic-bezier(.165, .84, .44, 1);
    }

    .tippy-popper[data-html] .btn, .tippy-popper[data-html] .btn:focus {
        margin-bottom: 0;
    }

    .tippy-tooltip[data-template-id="#my-template-id"] {
        padding: 2rem;
    }

    .tippy-link {
        white-space: nowrap;
    }

    .hero-subheading {
        font-weight: 300;
        font-size: 1.5rem;
        line-height: 1.7;
        color: white;
        margin-bottom: 2rem;
    }

    section {
        padding: 2rem 0;
    }

    #controlling-tooltips-manually .btn {
        margin-bottom: 1rem;
    }

    .btn.btn-secondary, .btn.btn-secondary:hover {
        background: #94a0b5;
        color: white;
        box-shadow: 0 3px 6px -1px rgba(0, 0, 0, 0.1), 0 15px 25px -4px rgba(100, 100, 150, 0.1);
    }

    hr {
        margin-top: 0;
        margin-bottom: 0.5rem;
        border-top: 1px solid rgba(0, 31, 255, 0.1);
    }

    #features {
        text-align: center;
    }

    #features h4 {
        font-size: 1.75rem;
        margin-bottom: 0.5rem;
    }

    #features p {
        max-width: 92%;
        margin: 0 auto;
    }

    .button-showcase {
        margin-bottom: 4rem;
    }

    .button-showcase .btn, .button-showcase .btn:active, .button-showcase:focus {
        margin: 1.25rem 1rem;
    }

    .btn.btn-gradient,
    .btn.btn-gradient:active,
    .btn.btn-gradient:focus {
        color: white;
        box-shadow: 0 3px 6px -1px rgba(0, 0, 0, 0.12), 0 10px 36px -4px rgba(77, 96, 232, 0.3);
        background: -webkit-linear-gradient(315deg,#73a5ff, #5477f5) !important;
        background: linear-gradient(135deg,#73a5ff, #5477f5) !important;
        letter-spacing: 0.5px;
        border: none;
        font-size: 1.2rem;
        padding: 0.35rem 1rem;
        transition: all 0.2s ease;
        -webkit-transform: translate3d(0, 0, 0);
        transform: translate3d(0, 0, 0);
    }

    section .btn, section .btn:active {
        margin: 0;
    }
    .btn.btn-gradient:hover,
    .btn.btn-gradient.tippy-active,
    .btn.btn-gradient:active {
        opacity: 0.75;
    }

    .heading-top {
        font-size: 2.5rem;
        color: #9079ff;
        font-weight: 600;
    }

    main .container-fluid {
        background: white;
        max-width: 1100px;
        padding: 2rem 1rem;
        box-shadow: 0 0 80px -4px rgba(0,20,40,0.1);
        -webkit-transition: opacity 1s ease;
        transition: opacity 1s ease;
    }

    table {
        font-size: 1rem;
    }

    .table-wrapper {
        position: relative;
    }

    .table-wrapper::after {
        content: '';
        position: absolute;
        top: 0;
        bottom: 0;
        right: 0;
        width: 25px;
        background: -webkit-linear-gradient(left, rgba(255,255,255,0.01), white);
        background: linear-gradient(90deg, rgba(255,255,255,0.01), white);
    }

    .tippy-tooltip.honeybee-theme {
        background-color: yellow;
        border: 2px solid orange;
        color: black;
    }
    .tippy-popper[x-placement^=top] .tippy-tooltip.honeybee-theme .tippy-arrow {
        border-top: 7px solid orange;
    }
    .tippy-popper[x-placement^=top] .tippy-tooltip.honeybee-theme .tippy-arrow::after {
        content: '';
        position: absolute;
        top: -8px;
        left: -6px;
        border-left: 6px solid transparent;
        border-right: 6px solid transparent;
        border-top: 6px solid yellow;
    }

    .tippy-tooltip.bordered-theme {
        border: 1px solid #bfc9dc;
        box-shadow: none !important;
    }
    .tippy-popper[x-placement^=top] .tippy-tooltip.bordered-theme .tippy-arrow {
        border-top: 7px solid #b4c0d5;
    }
    .tippy-popper[x-placement^=top] .tippy-tooltip.bordered-theme .tippy-arrow::after {
        content: '';
        position: absolute;
        top: -7px;
        left: -6px;
        border-left: 6px solid transparent;
        border-right: 6px solid transparent;
        border-top: 6px solid white;
    }

    .tippy-tooltip.gradient-theme {
        background: linear-gradient(45deg,#8c61f5, #ff9cad);
        font-weight: bold;
    }
    .tippy-tooltip.gradient-theme::after {
        position: absolute;
        left: 0;
        top: 5px;
        content: '';
        width: 100%;
        height: 100%;
        background: linear-gradient(45deg,#8c61f5, #ff9cad);
        -webkit-filter: blur(12px) brightness(1.2);
        filter: blur(12px) brightness(1.2);
        opacity: 0.8;
        font-weight: bold;
        z-index: -1;
    }

    .tippy-tooltip.colorshift-theme {
        background: linear-gradient(45deg,green,pink);
    }

    .test-element {
        width: 50px;
        height: 50px;
        border-radius: 50%;
        color: white;
        background: linear-gradient(#5172eb,#928aff);
        box-shadow: 0 10px 20px -5px rgba(0,20,60,0.1), 0 0 10px rgba(0,20,60,0.05);
        font-weight: 600;
        display: flex;
        justify-content: center;
        align-items: center;
        padding: 0.25rem 0.5rem;
        margin: 1%;
        cursor: default;
    }

    #performance-test {
        display: flex;
        justify-content: center;
        flex-wrap: wrap;
    }

    #performance-model {
        width: 70px;
    }

    #animated-tippy {
        animation: hover 2s ease-in-out infinite;
    }

    @@keyframes hover {
        0% {
            transform: translateY(0);
        }
        50% {
            transform: translateY(8px);
        }
        100% {
            transform: translateY(0);
        }
    }

    @@media (min-width: 576px) {
        main .container-fluid {
            padding: 2rem;
        }
        .hero-link span {
            color: #68f6ff;
            background: -webkit-linear-gradient(left,#bef9ff, #9aecff);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }
        a span {
            background: -webkit-linear-gradient(left,#00c0f9, #4d8cff, #9f72ff);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }
    }

    @@media (min-width: 768px) {
        html {
            font-size: 16px;
        }
        main .container-fluid {
            padding: 2.5rem 4rem;
            margin: 2rem auto;
            border-radius: 10px;
        }
        .content-wrapper {
            padding: 0 1%;
        }
        .hero {
            padding: 8rem 0 2rem;
        }
    }

    @@media (min-width: 976px) {
        main .container-fluid {
            padding: 2.5rem 5rem;
        }
    }

</style>


<link  rel="stylesheet" href="https://atomiks.github.io/tippyjs/tippy/themes.css" />

<script defer src="https://atomiks.github.io/tippyjs/js/focus-ring.js"></script>
<script async defer src="https://buttons.github.io/buttons.js"></script>




<link rel="stylesheet" href="@Url.Content("~/Scripts/CodeMirror/lib/codemirror.css")">
<script src="@Url.Content("~/Scripts/CodeMirror/lib/codemirror.js")"></script>
<script src="@Url.Content("~/Scripts/CodeMirror/mode/clike/clike.js")" type="text/javascript"></script>

<div id="ajaxContent">
    <div class="modal fade" id="myModal" tabindex="-1" role="dialog" aria-labelledby="myModalLabel">
        <div class="modal-dialog" role="document">
            <div class="modal-content">
                <div class="modal-body" id="modalContent">

                </div>
            </div>
        </div>
    </div>

    <h1>Novinky</h1>



    <script type="text/javascript">
        $(document).ready(function () {

            var counter = 0;
            var idList = new Array();

            var broadcastMessageCallback = function (from, msg) {
                for (var i = 0; i < idList.length; i++) {
                    chatboxManager.addBox(idList[i]);
                    $("#" + idList[i]).chatbox("option", "boxManager").addMsg(from, msg);
                }
            }


            // chatboxManager is excerpt from the original project
            // the code is not very clean, I just want to reuse it to manage multiple chatboxes
            chatboxManager.init({ messageSent: broadcastMessageCallback });

            $("#link_add").click(function (event, ui) {
                counter++;
                var id = "box" + counter;
                idList.push(id);
                chatboxManager.addBox(id,
                    {
                        dest: "dest" + counter, // not used in demo
                        title: "box" + counter,
                        first_name: "First" + counter,
                        last_name: "Last" + counter
                        //you can add your own options too
                    });
                event.preventDefault();
            });

        });
    </script>

    <div>
        <a href="@Url.Action("Vypis", "Home")">Vypis</a>
    </div>

    <a href="#" onclick="getAjaxContent()">ajax</a>
    <p><a id="link_add" href="#">Add Chatbox</a></p>

    @using (Html.BeginForm("Eval", "Home", FormMethod.Post, new { @class = "form-horizontal" })) // Umožní zpracovat nahrané soubory
    {
        <div class="form-group">
            <label class="col-sm-2 control-label">C#:</label>
            <div class="col-sm-10">
                @Html.TextArea("popis", @"// Na vstupu dostaneš List<int> inputList, obsahující n čísel. Vrať ho seřazený", new { @class = "form-control formatedtext", id = "myTextarea" })
            </div>
        </div>

        <div class="form-group">
            <div class="col-sm-offset-2 col-sm-10">
                <button type="submit" class="btn btn-default" onclick="openModelWindow()">Kompilovat</button>
            </div>
        </div>
    }

    <div class="content-wrapper">
        <div class="container-fluid">
            <section id="features">
                <p>Nahlédnout na profil AJAX:</p>
                <div id="html-showcase" class="button-showcase d-flex justify-content-center flex-wrap">
                    <button class="btn btn-gradient tippy" id="html-tippy" data-tippy-interactiveBorder="15" data-tippy-arrowType="round" data-tippy-trigger="click" data-tippy-theme="light" data-tippy-arrowTransform="scale(2, 2.5)" data-tippy-distance="25" data-tippy-animation="scale" data-tippy-interactive="true" data-tippy-arrow="true">Standa</button>
                </div>
            </section>
        </div>
    </div>


</div>

<style>
    .template{
        white-space:pre;
    }
</style>

<div id="template">
    <p  class="text-left">Uživatel <strong>Standa</strong> <small>(registrovaný 1.1.2018)</small></p>
    
    <div class="text-left">
        <small>Úroveň ověření: <b>F</b></small>
        <br />
        <small>Zkušenosti:     <b>2 / 100</b></small>
        <br />
        <small>Příspěvky:      <b>5</b></small>
        <br />
        <small>Reputace:       <b>0</b></small>
    </div>

    <br />
    <button class="btn btn-success tippy text-center" onclick="closeHtml()" title="Kdopak to je?" data-tippy-placement="bottom">
        Profil
    </button>
    <button class="btn btn-danger tippy text-center" onclick="closeHtml()" title="Tip: Podívej se na uživatelův profil" data-tippy-placement="bottom">
        Zavřít
    </button>
</div>

<script>
    var editor = CodeMirror.fromTextArea(myTextarea, {
        lineNumbers: true,
        mode: "text/x-csharp"
    });
</script>

<script type="text/javascript">
    function openModelWindow() {

        $('#myModal').modal('toggle');
    }
</script>

<script src="https://cdnjs.cloudflare.com/ajax/libs/jquery.form/4.2.2/jquery.form.min.js"></script>

<script>
    $('form').ajaxForm({
        target: '#modalContent'
    });
</script>

<script src="https://atomiks.github.io/tippyjs/tippy/tippy.js"></script>
<script src="https://atomiks.github.io/tippyjs/js/prism.js"></script>
<script src="https://atomiks.github.io/tippyjs/js/app.js"></script>